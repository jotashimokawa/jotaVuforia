using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class getPositionTest : MonoBehaviour
{
    public GameObject imageTarget_vinland;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       // Transform trans_vin = imageTarget_vinland.GetComponent<Transform>();
      //  Debug.Log(trans_vin.position);
        
    }
}
